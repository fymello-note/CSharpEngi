@model List<ProgettoProva.web.Models.Film.FilmViewModel>

@Html.ActionLink("registra film", "RegisterFilm", "Film", new { area = "" }, new { @class = "btn btn-primary mb-3" })

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Titolo</th>
            <th>Data di uscita</th>
            <th>Genere</th>
            <th>Regista</th>
            <th>Cast</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var film in Model)
        {
            <tr>
                <td>@film.Title</td>
                <td>@film.ReleaseDate.ToString("dd/MM/yyyy")</td>
                <td>@film.Genre</td>
                <td>@film.Director</td>
                <td>
                    <ul>
                        @foreach (var actor in film.Cast)
                        {
                            <li>@actor</li>
                        }
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>

